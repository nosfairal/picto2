{% extends 'base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/connexion.css') }}" type="text/css">
{% endblock %}
{% block body %}
    <div class="text-center mt-4">
        <a id="logo" class="px-3" href="{{ path('home') }}"><img src="{{ asset('img/LogoEcam.png') }}" alt="Ecam">
        </a>
    </div>
    <main id="homeWrapper" class="container my-4 py-4 px-5">
        <div class="my-5">Merci pour votre inscription ! Un email de confirmation va vous être envoyé.</div>
        <div class="d-flex justify-content-around my-3 py-3">
            <a href="{{ path('home') }}" class="btn py-3">Retour à l'accueil</a>
        </div>
    </main>

{% endblock %}